name: "tesouro_dbt"
version: "1.0.0"

profile: "tesouro_dbt"

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - target
  - dbt_packages

models:
  tesouro_dbt:

    intermediate:
      +materialized: view

    bridge:
      +materialized: table

    pit:
      +materialized: table

    dimensional:

      dimensions:
        +materialized: table

      facts:
        +materialized: table

    presentation:
      +materialized: table